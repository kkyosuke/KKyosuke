以下のSlackスレッドの会話内容を読み、参加した各ユーザー個人の進捗や決定事項として要約し、指定のJSON形式で出力してください。
最初のメッセージ（スレッド名）から対象となる日付を抽出してください。日付が明記されていない場合はメッセージの投稿日時から推測してください。
各ユーザーごとに、進捗のパーセンテージ（0〜100）と、5点満点での自己評価または客観的な評価スコアを算出して含めてください。

【重要な出力ルール】
- 進捗に関する情報が全く取得できそうにないユーザは要約（summaryリスト）から除外してください。
- もし全員が除外対象となり、要約すべき対象者が0人の場合でも、絶対に通常のテキストで返答しないでください。必ず指定されたJSON形式を維持し、`summary` を空の配列 `[]` として出力してください。

【会話内容】
{{threadContent}}
